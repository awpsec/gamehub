# dependencies
node_modules/

# server runtime data (SQLite DB, tokens, avatars) + secrets
data/
*.log
.env

# desktop build output (the installer is published as a GitHub Release asset,
# not committed — see README "Get the desktop client")
client/dist/

# generated: a copy of server/src for the desktop app's serverless mode
# (regenerated by client/scripts/sync-embedded-server.mjs before start/dist)
client/embedded/

# editor / assistant tooling
.claude/
.vscode/
.DS_Store
Thumbs.db
